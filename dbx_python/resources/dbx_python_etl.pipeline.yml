# The main pipeline for dbx_python

resources:
  pipelines:
    dbx_python_etl:
      name: dbx_python_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/dbx_python_etl"

      libraries:
        - glob:
            include: ../src/dbx_python_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
